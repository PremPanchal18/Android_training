<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo">
        <activity android:name=".Homepage"></activity>
        <activity android:name=".PreviewActivity"></activity>
        <activity android:name=".NextPageActivity"></activity>
        <activity android:name=".ServiceActivity"></activity>
        <activity android:name=".ListActivity"></activity>
        <activity android:name=".GalalryActivity"></activity>
        <activity android:name=".Services">


            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".Links" />
        <activity android:name=".list" />
        <activity android:name=".RecycleActivity" />
        <activity android:name=".TabActivity" /> <!-- <activity android:name=""></activity> -->
        <activity android:name=".datepicker" /> <!-- <activity android:name="."> -->
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".logout" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>